apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-notification
  namespace: cd
  labels:
    app: service-notification
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-notification
  template:
    metadata:
      labels:
        app: service-notification
    spec:
      containers:
        - name: service-notification
          image: image-registry.openshift-image-registry.svc:5000/cd/service-notification:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8084
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 10
            periodSeconds: 10
